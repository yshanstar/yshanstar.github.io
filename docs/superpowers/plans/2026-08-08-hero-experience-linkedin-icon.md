# Hero Experience and LinkedIn Icon Plan

**Goal:** Add the approved employer-neutral experience note and replace the large hero LinkedIn text button with an accessible icon link.

1. Add a failing static contract requiring the experience note and an `aria-label="Connect on LinkedIn"` icon link.
2. Update the hero markup and CSS with a steel-blue annotation and compact inline SVG LinkedIn icon, preserving `target` and `rel`.
3. Run all tests and visual checks; preserve game files.
